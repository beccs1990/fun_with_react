// import React from 'react';

// Get top repos list sorted by stars
// Get repo details when tapping on repo list item - last 10 PR's by author, name, # and status
// search for repos is probably not specific action? or will require search action
// const action = (type, payload) => ({ type, payload});

export const GET_REPOS_SUCCESS = 'GET_REPOS_SUCCESS';

// export const getTopRepos = () => {
//   action(GIT_REPOS.TOP_REPOS);
// };
//
// export const getRepoDetails = () => {
//   action(GIT_REPOS.REPO_DETAILS, payload)
// };
//
// //Can probably add creators in here as well.
